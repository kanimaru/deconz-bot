view:
  menu:
  {{- if .state.on }}
  state_row:
    on:
  {{- else }}
  state_row:
    off:
  {{- end }}
  {{- if .feature.brightness }}
  brightness_row:
    brightness:
  {{- end}}
  {{- if .feature.temperature }}
  temperature_row:
    temperature:
  {{- end }}
  {{- if .feature.color }}
  color_row:
    color:
  {{- end }}
