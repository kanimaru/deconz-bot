view:
  menu:
    close:
  {{- range $index, $group := .Groups }}
  row_{{$index}}:
    group_{{$group.Name}}:
      label: {{$group.Name}}
      click: "Select.Group"
      data: {{$group.Data}}
  {{- end}}